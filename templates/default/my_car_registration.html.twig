{% extends 'base.html.twig' %}

{% block title %}Immat Voiture{% endblock %}

{% block body %}

    <div class="mt-24 flex flex-col items-center justify-center">
        <h1 class="text-2xl font-bold">Mes Immatriculations</h1>
        <div class="flex flex-col">
            <p class="text-xl">{{ myCarRegistrations.siret }}</p>
            <p class="text-xl">{{ myCarRegistrations.firstname }}</p>
            <p class="text-xl">{{ myCarRegistrations.name }}</p>
        </div>
    </div>
{% endblock %}

